# AI 面试官 · 需求文档

> 版本：v3.2 · 三期已完成，四期主体完成 · 2026-08-19
> 状态：三期全部完成，四期全部完成（数字人形象市场待补充）

## 1. 项目概述

### 1.1 一句话定位

一款基于 AI 数字人的模拟面试平台：用户上传简历、选择岗位方向，由数字人面试官按题库知识点和简历内容进行语音/文字问答，结束后输出多维度点评报告。

### 1.2 核心价值

- **真实面试感**：数字人面试官形象 + 语音提问，贴近真实面试场景
- **知识驱动出题**：题库以知识文档形式沉淀，AI 基于检索到的知识点自主生成题目与参考答案，而非死记硬背固定题库
- **简历针对性**：解析简历后进行针对性追问，并给出人岗匹配评估
- **可量化反馈**：多维度雷达图评分 + 逐题点评 + PDF 报告导出

### 1.3 当前阶段

二期已完成（P1–P4，共 13 项需求全部完成）。

- **P1**: 体验增强与陪练完善（语音输入、全屏、摄像头、简历报告、分享链接、岗位扩展）
- **P2**: 用户体系与配置化（注册登录、ASR/TTS 配置、数字人形象选择）
- **P3**: 代码与算法能力（算法练习模块、面试算法题判题）
- **P4**: 基建升级与回放（PostgreSQL 迁移、文件存储抽象、面试录制回放）

**四期已完成**：知识库体系升级、设置/管理体系重构、核心体验增强、商业化与增长。

**后续可选**：数字人形象市场（更多数字人形象可选）。

***

## 2. 目标用户与场景

| 维度   | 说明                                                                            |
| ---- | ----------------------------------------------------------------------------- |
| 目标用户 | 求职者、技术自学者、希望模拟真实面试体验的人群                                                       |
| 核心场景 | 用户上传简历 → 选择岗位/难度/时长 → 数字人面试官提问 → 用户语音或文字作答 → AI 追问 → 结束后生成点评报告                |
| 使用环境 | 桌面浏览器（Chrome/Edge 94+、Firefox 130+）与移动端浏览器（iOS Safari 15+、Android Chrome 94+） |
| 使用频次 | 单次面试 15–45 分钟，可反复练习                                                           |

***

## 3. 功能需求

### 3.1 题库管理（知识文档）

**定位**：题库是"知识库"，不是"固定题库"。AI 通过 RAG 检索相关知识，自主出题、判卷、给参考答案。

| 需求项   | 说明                                             |
| ----- | ---------------------------------------------- |
| 上传    | 网页上传 Markdown（.md）文件，支持批量上传多个文档                |
| 解析与分块 | 后端解析 Markdown，按标题层级切分为语义块，向量化后存入 Chroma 向量库    |
| 知识检索  | 面试过程中，AI 根据当前岗位/难度/对话上下文，从向量库检索相关知识块，作为出题和判卷依据 |
| 题库列表  | 展示已上传的知识文档列表，支持查看、删除                           |
| 标签与岗位 | 上传时可标注所属岗位方向（前端/后端/算法…）与难度等级，作为检索过滤条件          |
| 版本管理  | 文档处理完成自动创建快照，支持手动保存版本、版本预览、回滚（回滚前自动快照当前状态）、删除版本 |
| 协作编辑  | 独占编辑锁（30 分钟过期），三级协作者权限（只读/可编辑/管理员），协作者管理           |
| 通知     | 协作者编辑提醒、审核结果通知、系统公告                             |

**题库 Markdown 格式建议**：

```markdown
---
title: Vue 性能优化
position: 前端
difficulty: 中级
tags: [Vue, 性能优化, Webpack]
---

# Vue 性能优化

## 一、代码层面的优化

### 1.1 v-if 和 v-show 区分使用场景

v-if 是真正的条件渲染……v-show 基于 CSS display 切换……

### 1.2 computed 和 watch 区分使用场景

computed 是计算属性，有缓存……watch 用于观察监听……

## 二、Webpack 配置层面的优化
...
```

- frontmatter 的 `position` / `difficulty` / `tags` 用于检索过滤
- 标题层级（`#` / `##` / `###`）作为分块边界
- 正文为知识内容，AI 据此生成题目

### 3.2 简历管理

| 需求项  | 说明                                    |
| ---- | ------------------------------------- |
| 格式支持 | PDF、Word（.docx）                       |
| 上传   | 面试配置页上传，一份简历可复用于多次面试                  |
| 解析   | 后端提取简历文本内容（项目经历、技能、教育背景等）             |
| 轻量分析 | 上传后即时给出轻量简历分析报告：结构完整度评估、与目标岗位匹配度初评    |
| 用途   | 面试中作为 AI 针对性提问与追问的素材；终评中作为"人岗匹配"维度的输入 |
| 简历评分 | 终评中独立给出简历评分维度（区别于轻量分析：终评结合面试表现综合判断）   |

### 3.3 面试配置

用户开始一场面试前，需填写以下配置：

| 配置项   | 必选 | 说明                                       |
| ----- | -- | ---------------------------------------- |
| 岗位方向  | ✅  | 来自题库标签（前端/后端/算法/产品…），决定知识检索范围            |
| 难度    | ✅  | 初级 / 中级 / 高级，决定出题深度与追问策略                 |
| 时长    | ✅  | 15 / 30 / 45 分钟，默认 30 分钟；到时后 AI 自动收尾并出点评 |
| 面试官风格 | ⬜  | 严肃 / 温和 / 压力面，影响提问语气与追问强度（对 prompt 成本极低） |
| 简历    | ⬜  | 可选上传简历以启用针对性提问                           |
| 面试模板 | ⬜  | 预置 5 个模板（前端基础/前端进阶/算法/后端/产品），支持自定义模板创建，一键复用 |
| 倒计时   | 自动  | 面试过程中实时显示剩余时间，最后 60 秒红色闪烁警告               |

### 3.4 面试进行

#### 3.4.1 面试官形象

- **主模式（数字人）**：集成 XingyunAvatarAgent 数字人 SDK，数字人形象出题并语音播报
- **降级模式（Lottie 动画）**：当浏览器不支持 WebGL2/WebCodecs 或 SDK 加载失败时，自动降级为 Lottie/CSS 动效面试官形象 + 说话动效 + 字幕文字
- 两种模式共享同一组件接口（接收"开始说话/停止说话/播放文本"事件），业务层无感知

#### 3.4.2 提问与回答

| 需求项  | 说明                                 |
| ---- | ---------------------------------- |
| 出题来源 | 题库知识文档（RAG 检索）+ 简历内容，混合出题          |
| 提问方式 | 数字人语音播报 + 同步显示文字字幕                 |
| 回答方式 | 语音（主）：麦克风录音 → ASR 转文字；文字（兜底）：文本框输入 |
| 追问   | 每题最多 2 层追问，AI 根据回答质量决定是否追问及追问内容    |
| 时长控制 | 到达设定时长后，AI 自动收尾（不再出新题），进入点评阶段      |
| 提前结束 | 用户可随时点击"结束面试"，立即进入点评阶段             |
| 打断   | 用户可在面试官说话时打断（点击按钮或开始语音输入），进入作答     |
| 实时字幕 | 面试官提问与用户回答均以对话气泡形式实时显示             |

#### 3.4.3 面试状态机

```
[等待开始]
    ↓ 用户点击"开始面试"
[面试官提问中] ← 数字人播报题目
    ↓ 播报完成 / 用户打断
[等待用户回答] ← 语音 ASR 或文字输入
    ↓ 用户提交回答
[AI 分析中] ← LLM 评估回答 + 决定追问/换题
    ├─ 需要追问 → [面试官追问中] → [等待用户回答]（追问层级 +1，最多 2 层）
    ├─ 换题 → [面试官提问中]（下一题）
    └─ 时长到 / 无更多题 / 用户结束 → [生成点评中]
    ↓
[点评报告]
```

### 3.5 点评报告

#### 3.5.1 报告内容

| 板块    | 说明                                                   |
| ----- | ---------------------------------------------------- |
| 总评    | 整体表现概述，1–2 段文字                                       |
| 雷达图评分 | 多维度评分（0–100），维度含：专业知识、表达逻辑、人岗匹配、应变能力、技术深度            |
| 逐题点评  | 每题：题目原文、用户回答、AI 点评（亮点 + 不足 + 改进建议）、参考答案（基于检索到的知识点生成） |
| 简历评分  | 结构完整度、与目标岗位匹配度、亮点与短板                                 |
| 人岗匹配  | 结合简历与面试表现的综合匹配评估                                     |
| 总分    | 加权汇总分                                                |

#### 3.5.2 报告操作

- 查看：面试结束后即时展示
- 保存：自动保存到历史记录（按匿名用户 ID 归属）
- 导出：支持导出为 PDF 文件
- 历史：历史面试列表，可回看任意一场报告
- 多轮对比：选择多次面试报告进行纵向对比，展示表现变化趋势

### 3.6 LLM 与数字人配置管理

| 需求项     | 说明                                                        |
| ------- | --------------------------------------------------------- |
| 管理页入口   | 独立路由 `/admin`，支持旧 admin\_token（口令引导）或用户 role=admin 鉴权进入          |
| LLM 配置项 | 供应商 base\_url、api\_key、model 名称，可保存多套，启用其中一套              |
| 默认值     | DeepSeek（`https://api.deepseek.com`），后续支持其他 OpenAI 兼容供应商  |
| 数字人配置   | 保存多套 ASR/TTS 凭证（appId/appSecret/gatewayServer/avatar\_image），与 LLM 同级管理 |
| 安全      | api\_key 仅存后端，前端不暴露；管理页口令校验                               |
| 用户管理    | 管理员可提升/降级用户角色、设置用户配额（套餐、面试次数、知识库文档数、AI 调用次数）               |
| 邀请码管理   | 管理员可生成邀请码（指定套餐、使用次数），用户兑换后升级套餐                              |
| 数据看板    | 平台运营数据可视化：用户数、面试数、报告数、知识库文档数、版本数、套餐分布                        |

### 3.7 用户体系

| 需求项  | 说明                                           |
| ---- | -------------------------------------------- |
| 注册登录 | 用户名+密码注册/登录，密码 PBKDF2 加盐哈希，令牌 HMAC-SHA256 签名 |
| 用户隔离 | 简历/面试/对话/代码提交按 user\_id 隔离，数据互不可见            |
| 知识文档 | 支持私有 + 公有文档：私有仅自己可见，公有所有用户可检索                |
| 历史数据 | 首次注册用户自动继承匿名期的历史数据（简历/面试/对话）                 |
| RBAC 权限 | 用户 role 字段（user/admin），管理员可管理用户、配置、查看统计          |
| 个人设置 | 默认岗位方向、语言切换（中/英）、主题切换（亮/暗）、通知偏好设置            |
| 使用配额 | 免费用户面试 10 次、知识库 20 篇、AI 调用 100 次；标准/企业套餐更高配额     |

### 3.8 代码与算法练习

| 需求项   | 说明                                                            |
| ----- | ------------------------------------------------------------- |
| 练习模块  | 独立路由 `/code`，左侧题目描述、中间 Monaco 编辑器、右侧输出面板                      |
| 多语言支持 | Python / JavaScript / TypeScript / Java / C++ / C / Go / Rust |
| 代码运行  | 通过 Judge0 CE API 在沙箱中运行代码，返回 stdout/stderr                    |
| 判题系统  | 多测试用例判题，输出通过数/总数（pass/total），支持 WA/CE/RE/TLE 状态               |
| AI 提示 | 卡住时可请求 AI 提示，不直接给答案                                           |
| 提交历史  | 代码提交记录，可回看每次提交的代码、结果、判题详情                                     |
| 面试算法题 | 面试中自动插入算法题环节，用户在面试内可编辑代码并提交判题，判题后自动进入下一题                      |

### 3.9 面试录制与回放

| 需求项  | 说明                                          |
| ---- | ------------------------------------------- |
| 面试录制 | 面试中可点击"录制"按钮，通过 MediaRecorder API 录制音频      |
| 自动上传 | 录制片段按时间分片，自动上传到后端存储                         |
| 存储方式 | 支持本地文件系统（默认）和 S3 兼容对象存储（可选配置）               |
| 回放页面 | 独立路由 `/interview/{id}/playback`，列出所有录制片段并播放 |
| 历史入口 | 每条面试记录右侧显示"回放"按钮                            |

### 3.10 通知系统

| 需求项   | 说明                                              |
| ----- | ----------------------------------------------- |
| 通知类型  | 协作通知（协作者编辑提醒）、审核通知（文档审核结果）、系统公告（版本更新等）            |
| 通知中心  | 导航栏铃铛图标，未读红点徽章，点击展开通知列表                        |
| 已读管理  | 单条标记已读 / 全部标记已读                                  |
| 偏好设置  | 用户可在个人设置中按类型开关通知（协作/审核/系统）                       |

### 3.11 商业化与增长

| 需求项    | 说明                                             |
| ------ | ---------------------------------------------- |
| 套餐体系   | free（免费）/ standard（标准）/ enterprise（企业）三档          |
| 使用配额   | 按套餐限制面试次数、知识库文档数、AI 调用次数，前端实时展示已用/总量              |
| 邀请码    | 管理员可生成邀请码（指定套餐、最大使用次数），用户兑换后升级套餐权益              |
| 数据分析看板 | 管理员专属：用户总数、管理员数、面试总数、报告总数、知识库文档数、版本数、各套餐用户分布    |

***

## 4. 非功能需求

### 4.1 响应式与兼容性

| 端  | 浏览器                               | 说明                                    |
| -- | --------------------------------- | ------------------------------------- |
| 桌面 | Chrome/Edge 94+、Firefox 130+      | 完整功能                                  |
| 移动 | iOS Safari 15+、Android Chrome 94+ | 完整功能，布局自适应                            |
| 降级 | 不支持 WebGL2/WebCodecs 的浏览器         | 数字人降级为 Lottie 动画，语音降级为 Web Speech API |

响应式断点：

- 移动端：`< 768px`（单列布局，数字人区域顶部，对话区域下方）
- 平板：`768px – 1024px`
- 桌面：`> 1024px`（左右分栏：左侧数字人，右侧对话）

### 4.2 性能

- 首屏加载 ≤ 3 秒（数字人 SDK 资源可懒加载，面试配置页不阻塞）
- 面试对话响应延迟 ≤ 5 秒（含 LLM 生成 + 数字人首帧渲染）
- 简历解析 ≤ 3 秒
- 题库向量化为离线任务，上传后异步处理，不阻塞用户

### 4.3 安全

- 数字人凭证（appId/appSecret/gatewayServer）仅存后端 `.env`，前端通过后端代理获取 SDK 初始化参数
- LLM api\_key 仅存后端，前端不直连 LLM
- 管理页口令保护
- 麦克风权限需 HTTPS 或 localhost（部署时强制 HTTPS）

### 4.4 可扩展性

- LLM 供应商：OpenAI 兼容协议抽象，可一行切换
- ASR/TTS：Provider 接口抽象，可切换 SDK 默认 / 第三方 / Web Speech
- 数字人 / 降级形象：统一组件接口，可无缝切换
- 数据库：SQLModel ORM，可从 SQLite 一行配置切 PostgreSQL

***

## 5. 用户流程

### 5.1 首次使用流程

```
进入首页
  → 新手引导（5 步介绍：欢迎 → 简历/JD → 面试模板 → 开始面试 → 查看报告）
  → 上传题库知识文档（.md）
  → 上传简历（PDF/Word）【可选】
  → 选择面试模板或自定义配置（岗位/难度/时长/风格）
  → 点击"开始面试"
  → 数字人初始化 + 首题生成
  → 面试进行（提问→回答→追问→换题 循环，倒计时实时显示）
  → 时长到 / 用户结束
  → 生成点评报告
  → 查看报告 / 多轮对比 / 导出 PDF / 保存历史
```

### 5.2 历史回看流程

```
首页 → 历史记录列表 → 选择某场面试 → 查看报告详情
```

### 5.3 管理配置流程

```
访问 /admin → 口令验证或管理员登录 → 配置 LLM 供应商 → 保存 → 启用
                                    → 配置数字人凭证 → 保存 → 启用
                                    → 用户管理：提升/降级角色、设置配额
                                    → 邀请码管理：生成邀请码 → 用户兑换
                                    → 数据看板：查看运营统计
```

***

## 6. 数据模型概要

> 详细的表结构见《技术方案》数据库设计章节。此处列出核心实体。二期新增模型以 ★ 标记。

| 实体          | 说明                     | 关键字段                                                                                                  |
| ----------- | ---------------------- | ----------------------------------------------------------------------------------------------------- |
| 用户（★ 登录版）   | 支持用户名密码注册登录            | id, username, password\_hash, anonymous\_uuid, preferred\_avatar\_config\_id, role, preferred\_position, language, theme, notification\_settings, created\_at |
| 知识文档        | 上传的题库 Markdown，支持私有/公有 | id, user\_id, filename, position, difficulty, tags, content, status, is\_public                       |
| 知识块         | 文档分块向量化后的块             | id, doc\_id, chunk\_index, content, embedding\_id                                                     |
| 知识版本（★ 四期） | 文档全量快照，支持版本管理与回滚     | id, doc\_id, version\_number, content, title, position, difficulty, tags, change\_note, created\_by, created\_at |
| 编辑锁（★ 四期）   | 知识库独占编辑锁，30 分钟过期       | id, doc\_id, user\_id, locked\_at, expires\_at                                                        |
| 协作者（★ 四期）   | 知识库协作者权限（read/edit/admin） | id, doc\_id, user\_id, permission, created\_at                                                        |
| 通知（★ 四期）     | 站内通知（协作/审核/系统）          | id, user\_id, type, title, content, is\_read, created\_at                                             |
| 面试模板（★ 四期） | 保存常用面试配置，一键复用          | id, user\_id, name, position, difficulty, duration, style, resume\_id, jd\_id, avatar\_config\_id, code\_enabled, created\_at |
| 用户配额（★ 四期） | 免费/标准/企业套餐使用配额          | id, user\_id, plan, interview\_limit, interview\_used, knowledge\_limit, knowledge\_used, ai\_calls\_limit, ai\_calls\_used, expires\_at |
| 邀请码（★ 四期）   | 邀请/推荐码，兑换套餐权益           | id, code, owner\_user\_id, reward\_amount, is\_used, used\_by, created\_at                             |
| 简历          | 用户上传的简历                | id, user\_id, filename, file\_path, parsed\_text, analysis\_result                                    |
| 面试记录        | 一场面试的完整记录              | id, user\_id, resume\_id, position, difficulty, duration, style, status, started\_at, ended\_at       |
| 面试对话        | 逐条对话消息                 | id, interview\_id, role（面试官/用户）, content, question\_index, followup\_level, created\_at               |
| 点评报告        | 面试终评                   | id, interview\_id, total\_score, dimension\_scores（JSON）, summary, per\_question\_reviews（JSON）       |
| LLM 配置      | 供应商配置                  | id, name, base\_url, api\_key（加密）, model, is\_active                                                  |
| 数字人提供商配置（★） | ASR/TTS 凭证集合           | id, name, app\_id, app\_secret（加密）, gateway\_server, avatar\_image, is\_active                        |
| 数字人形象（★）    | 可选的数字人形象               | id, name, cover\_url, provider\_config\_id, extra（JSON）, is\_active                                   |
| 算法题目（★）     | 代码/算法练习的题目             | id, title, description, difficulty, position, tags, examples, test\_cases, time\_limit, memory\_limit |
| 代码提交记录（★）   | 用户代码提交 + 判题结果          | id, user\_id, problem\_id, language, code, status, stdout, stderr, pass\_count, total\_count          |
| 录制文件（★）     | 面试回放音视频片段              | id, interview\_id, user\_id, stream\_type, file\_key, file\_size, duration\_ms, mime\_type            |

***

## 7. 范围说明

### 7.1 MVP 范围（已完成）

- ✅ 题库上传 + RAG 检索
- ✅ 简历上传 + 解析 + 轻量分析
- ✅ 面试配置 + 面试进行（数字人 + 降级）
- ✅ 语音 + 文字回答
- ✅ 点评报告 + PDF 导出 + 历史记录
- ✅ LLM 管理页（口令保护）
- ✅ 桌面 + 移动端响应式

### 7.2 二期范围（已完成）

二期共 13 项，按依赖、主题、工作量与风险拆分为 4 个迭代（P1→P4）。排序原则：先做建立在已交付能力之上、用户可见价值高、低风险的项；用户体系等大地基单独成期；全新大模块与基建升级放后段。

#### 二期 P1 · 体验增强与陪练完善

> 建立在本轮已交付的 chat / ASR / TTS / 数字人之上，快速见效、低风险。均不依赖登录体系。

- ✅ 数字人陪练增加语音输入（chat ASR 麦克风输入，已完成）
- ✅ 数字人陪练模块增加全屏功能（Fullscreen API，全屏仅保留数字人，通过 ASR 与数字人交互，展示 ASR 字幕与数字人字幕）
- ✅ 面试对标牛客：增加面试者摄像头视频；进入面试前调试摄像头与麦克风，确保正常，提升仿真度
- ✅ 简历独立评分报告（MVP 仅纳入终评维度，二期独立成报告页面 `/resume/:id/report`）
- ✅ 分享面试报告链接（公开只读路由 `/share/:token`，7 天有效期）
- ✅ 岗位方向增加测试、测试开发等其他岗位（FALLBACK\_POSITIONS 扩展）

#### 二期 P2 · 用户体系与配置化

> 多用户地基 + 个性化/配置化。数据模型已按多用户预留，补登录即可激活。

- ✅ 用户登录注册体系（激活 user\_id，简历/面试/历史/会话按用户隔离，支持 PBKDF2 密码哈希 + HMAC 令牌）
- ✅ LLM 管理页 UI 配置多套 ASR/TTS（AvatarProviderConfig 模型，镜像 LLMConfig 管理逻辑）
- ✅ 多套数字人形象选择（Avatar 模型 + HomeView 形象选择界面）

#### 二期 P3 · 代码与算法能力

> 全新大模块。判题系统与算法练习同期建设。

- ✅ 代码/算法练习模块（独立路由 `/code`，Monaco 编辑器 + Judge0 CE 判题 + AI 提示 + 提交历史）
- ✅ 面试增加算法题（面试中 algorithm action 自动选题，内嵌代码编辑器，判题后自动进入下一题）

#### 二期 P4 · 基建升级与回放

> 基础设施加固 + 媒体录制。支撑规模化与复盘体验。

- ✅ 后端切换 PostgreSQL（支持环境变量一键切换，迁移脚本 `server/scripts/migrate_to_pg.py`）
- ✅ 云端文件存储（S3 兼容对象存储抽象层 `storage_service.py`，不配置时自动使用本地存储）
- ✅ 面试回放（音视频录制，MediaRecorder API → 分段上传 → 后端存储 → 回放页面播放）

### 7.3 三期范围（已完成）

#### 三期 P1 · 跨端集成（已完成）

- ✅ Capacitor 集成：iOS/Android 双平台支持，WebView 打包，一套 Web 代码生成原生 App
- ✅ 微信小程序壳工程：原生壳 + `<web-view>` 嵌入 H5，JSSI 双向通信桥接
- ✅ 设备检测 composable（useDevice）：移动端/桌面端、iOS/Android、触摸屏、屏幕方向
- ✅ H5↔宿主通信桥接（bridge.ts）：分享、关闭页面、消息总线
- ✅ Capacitor Preferences Token 存储：移动端安全存储
- ✅ 多环境 API 配置：web/capacitor/miniprogram 三套 .env 文件
- ✅ 移动端底部 TabBar：MobileTabBar 组件（首页/聊天/面试/历史/我的）
- ✅ 移动端导航适配：桌面端保留完整导航，移动端隐藏代码练习入口
- ✅ 数字人降级策略：移动端 WebGL 检测 → Lottie 动画降级

#### 三期 P2 · 移动端 UI 优化（已完成）

- ✅ 底部安全区适配：修复 MobileTabBar 遮挡页面内容问题
- ✅ Chat 页面输入区可见性：调整容器高度计算，确保输入框不被 TabBar 遮挡
- ✅ 消息气泡换行修复：移动端 markdown 内容正确换行，长代码/长 URL 不溢出
- ✅ 移动端首页/面试页样式优化：头像选择卡片响应式尺寸、CTA 按钮触控目标、摄像头 PiP 安全区适配
- ✅ 移动端表单元素优化（touch target ≥ 44px）：所有按钮/输入框符合移动端触控标准
- ✅ 移动端横竖屏适配：useDevice isLandscape 检测，面试页横屏时头像区与对话区左右分栏
- ✅ 小程序端样式专项适配：wx-env body class、-webkit-tap-highlight-color 禁用、100dvh 视口、滚动惯性优化

#### 三期 P3 · 移动端功能完善（已完成）

- ✅ 移动端专属个人中心页面（ProfileView：用户信息、面试统计、快捷导航、原生分享）
- ✅ 移动端面试配置页优化（InterviewSetupView：响应式布局、44px 触控目标、移动端间距）
- ✅ 移动端报告查看页优化（ReportView：响应式标签、分享区域纵向堆叠、触控目标优化）
- ✅ 移动端历史记录页优化（HistoryView：卡片式布局、触控目标、移动端间距）
- ✅ 原生分享面板集成（bridge.ts shareContent：Capacitor Share + Web Share API 降级）
- ✅ 移动端文件选择器（useMobile.ts：Web `<input type="file">` 方案，兼容所有环境）

### 7.4 四期范围（已完成）

#### 四期 P1 · 知识库体系升级（已完成）

- ✅ 知识库按技术方向分组展示（前端/后端/算法/测试等）：Tab 切换 + 文档数量徽章
- ✅ 知识库支持标签筛选与搜索：全文搜索（标题/标签/方向）+ 标签多选筛选
- ✅ 知识库版本管理与变更记录：全量快照存储、自动快照（文档处理完成时）、手动版本标签、版本查看/预览/回滚/删除
- ✅ 知识库协作：多人协同编辑与审核流程（独占编辑锁 30 分钟过期、三级权限 read/edit/admin、协作者管理、站内通知）

#### 四期 P2 · 设置/管理体系重构（已完成）

- ✅ 设置页面分层：个人设置（所有用户）/ 平台管理（仅管理员）
- ✅ 个人设置：偏好的数字人形象、默认岗位方向、语言切换（中/英）、主题切换（亮色/暗色）、通知分类开关（协作/审核/系统）
- ✅ 平台管理：LLM 配置、数字人凭证、知识库管理、用户管理（仅管理员）
- ✅ 权限体系：区分管理员和普通用户的功能可见性（RBAC role 字段、require_admin 中间件、前端路由守卫）

#### 四期 P3 · 核心体验增强（已完成）

- ✅ 面试引导/新手教程：首次使用引导流程（5 步引导、进度指示、可跳过、localStorage 记忆）
- ✅ 面试倒计时提醒：时长进度可视化、最后 60 秒红色闪烁警告、到时自动结束
- ✅ 多轮面试对比报告：多次面试表现纵向对比（ComparisonResult 数据结构）
- ✅ 面试模板：保存常用面试配置，一键复用（5 个内置模板 + 自定义模板创建）
- ⬜ 数字人形象市场：更多数字人形象可选（待后续补充）

#### 四期 P4 · 商业化与增长（已完成）

- ✅ 账户体系升级：套餐/订阅（free/standard/enterprise 三档）
- ✅ 使用配额管理：免费用户次数限制（面试次数、知识库文档数、AI 调用次数）
- ✅ 邀请/推荐系统：邀请码生成、兑换码兑换套餐权益、使用次数限制
- ✅ 数据分析看板：平台运营数据可视化（用户数、面试数、报告数、知识文档数、版本数、套餐分布）

